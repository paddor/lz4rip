# lz4rip-decode

LZ4 block decompression and no-output validation for
[lz4rip](https://crates.io/crates/lz4rip).

This crate is an internal building block. Use `lz4rip` directly unless you need only the decompressor.
